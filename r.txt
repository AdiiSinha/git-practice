I need you to fix the Commitment Intelligence feature in my existing Workday Copilot application.

IMPORTANT:
I have attached/provided these files:
- main.py
- Commit.jsx
- graph.js
- screenshot/reference of the current Commitment UI

DO NOT redesign the UI.
DO NOT replace the current Commit.jsx UI with a simpler UI.
DO NOT change main.jsx.
DO NOT introduce dummy/synthetic commitment data.
Use the real Microsoft Graph Outlook data already being fetched by the application.

====================================================
CURRENT PROBLEM
====================================================

The Commitment page UI is already implemented and looks correct.

The browser console proves that Microsoft Graph data is reaching Commit.jsx:

Sent count: 4
Inbox count: 7

Example real SENT email data:

SENT 1:
subject: "Commit checks"
body:
"I will send you to tomorrow"

SENT 2:
subject: "Commit check"
body:
"I'll sent to you soon"

SENT 3:
subject: "Re: sneha test"
body:
"Hi Ghosal, Sneha, Badam khayegi ?"

SENT 4:
subject: "Urgent test 2"
body:
"Important testing part 2"

The frontend then calls:

POST /api/commitments

The backend successfully responds, but the response is:

{
  "generatedAt": "...",
  "summary": {},
  "commitments": []
}

Therefore Graph retrieval is working.

The problem is that the Commitment AI/backend is returning ZERO commitments even though the first two sent emails clearly contain first-person commitment intent.

====================================================
CURRENT Commit.jsx CONTRACT
====================================================

Commit.jsx currently contains:

async function analyzeAI(sent, inbox, employee){
  const r = await fetch(`${API}/api/commitments`, {
    method:"POST",
    headers:{"Content-Type":"application/json"},
    body:JSON.stringify({
      sent,
      inbox,
      employee,
      now:new Date().toISOString()
    })
  });

  const d = await r.json();

  if(!r.ok)
    throw Error(d.detail || "Commitment AI failed");

  return d;
}

The frontend expects:

d.commitments

and each commitment should contain fields compatible with the existing UI, including:

id
title
action
status
confidence
confidenceReason
priority
recipientName
recipientEmail
project
dueAt
dueLabel
promisedAt
originalQuote
sourceId
sourceSubject
sourceWebLink
completionDetected
completionEvidence
completionSubject
completionAt
risk
riskReason

Do NOT change these field names unless absolutely necessary.

====================================================
WHAT I WANT YOU TO DO
====================================================

First inspect ALL THREE files together:

1. main.py
2. Commit.jsx
3. graph.js

Trace the complete data flow:

Microsoft Graph
    ↓
getSentMail()
    ↓
Commit.jsx
    ↓
POST /api/commitments
    ↓
main.py
    ↓
AI/model
    ↓
structured commitments
    ↓
Commit.jsx
    ↓
existing Commitment UI

Find the actual reason why:

commitments: []

is being returned.

Do not guess.

Fix the actual backend/frontend contract.

====================================================
COMMITMENT DETECTION REQUIREMENTS
====================================================

The system must detect genuine employee commitments from the user's SENT Outlook emails.

Examples that MUST be detected:

"I will send you the document tomorrow."

"I'll send it to you soon."

"I will review this by EOD."

"I'll get back to you tomorrow."

"I can share the report this afternoon."

"I will prepare the presentation."

"I'll follow up with the client."

"I will check this and update you."

The system should distinguish these from ordinary conversational emails.

For example:

"Hi Sneha, how are you?"
should NOT become a commitment.

"Thanks for the update."
should NOT become a commitment.

"Important testing part 2"
should NOT automatically become a commitment.

====================================================
HYBRID AI + DETERMINISTIC REQUIREMENT
====================================================

Use a reliable hybrid approach.

AI should perform semantic commitment extraction and reasoning.

However, if the AI returns zero commitments while the SENT email contains an obvious first-person commitment such as:

"I will..."
"I'll..."
"I can..."
"I plan to..."
"I intend to..."
"Let me..."

then use a deterministic fallback/extraction step so obvious commitments are not lost.

The deterministic fallback must use the REAL SENT Outlook messages supplied to the endpoint.

Do NOT create fake/sample commitments.

Do NOT hardcode the current example emails.

The fallback must work for future real emails as well.

====================================================
STRUCTURED RESPONSE REQUIREMENT
====================================================

The /api/commitments endpoint MUST ALWAYS return valid JSON in this shape:

{
  "generatedAt": "...",
  "summary": {
    "active": 0,
    "dueToday": 0,
    "overdue": 0,
    "completed": 0,
    "noDeadline": 0
  },
  "commitments": []
}

Every commitment must be an object matching the frontend contract.

Do not return a natural-language paragraph instead of structured JSON.

Do not put JSON inside a markdown code block.

If an LLM is being used, make the backend robust against:
- markdown fences
- invalid JSON
- empty responses
- missing fields
- malformed model output

Normalize/validate the model response before returning it to Commit.jsx.

====================================================
DEADLINE DETECTION
====================================================

Detect deadlines from the actual email text.

Examples:

"tomorrow"
→ dueLabel: "Tomorrow"

"today"
→ dueLabel: "Today"

"EOD"
→ appropriate today deadline

"by Friday"
→ calculate the appropriate date

"next week"
→ appropriate future date/range

"soon"
→ due_soon

No explicit deadline
→ status: "no_deadline"

Do not invent a deadline when none exists.

====================================================
SOURCE / EVIDENCE
====================================================

Every commitment must preserve its source email.

Use:

sourceId
sourceSubject
sourceWebLink
originalQuote
promisedAt

The originalQuote should contain the actual sentence/phrase from the real Outlook email that caused the commitment to be detected.

Example:

originalQuote:
"I will send you to tomorrow"

Do NOT generate fake quotes.

The existing "Details" drawer must continue to work using sourceId and getMailMessage().

====================================================
COMPLETION DETECTION
====================================================

Keep the existing completion-detection architecture.

If a later email indicates that the commitment was completed, preserve:

completionDetected
completionEvidence
completionSubject
completionAt

Do not remove this functionality while fixing initial commitment extraction.

====================================================
SUMMARY
====================================================

The backend must calculate summary from the FINAL commitment list returned to the frontend.

For example, if two active commitments are detected:

summary should contain corresponding counts.

Do not leave summary as {} when commitments exist.

====================================================
CURRENT UI MUST REMAIN THE SAME
====================================================

This is extremely important.

The current Commit.jsx UI is already designed and should remain visually/functionally intact.

Keep:

- "MEMORY & FOLLOW-UP"
- "Your commitments"
- "LIVE GRAPH + AI"
- "Refresh analysis"
- KPI cards
- Active
- Due today
- Overdue
- Completed
- No deadline
- AI attention banner
- All / Due today / Due soon / Overdue / Completed tabs
- Search commitments
- Everyone filter
- Project filter
- Commitment cards
- AI confidence
- HIGH PRIORITY
- recipient
- project
- deadline
- original quote
- Details
- Open email
- Complete
- Draft follow-up
- Ask AI
- completion evidence
- risk indicator
- commitment evidence drawer
- original email section
- AI reasoning
- Draft follow-up modal
- Save Outlook draft
- Approve & Send
- Human approval requirement
- existing animations
- existing CSS

Do NOT replace the UI with a new design.

Do NOT remove functionality to make the bug disappear.

The screenshot I provided is the visual reference. Preserve this appearance.

====================================================
REAL GRAPH DATA ONLY
====================================================

Do not use:
- tasks from data.js
- hardcoded commitments
- synthetic Teams data
- fake email examples
- sample commitment arrays

The Commitment page must be driven by:

getSentMail()
getMail()
getProfile()

and the real Outlook messages.

The current application already proves:

4 sent messages
7 inbox messages

are being retrieved successfully.

Do not break Microsoft Graph authentication.

Do not change the MSAL authentication architecture.

Do not change main.jsx.

====================================================
GRAPH.JS
====================================================

Inspect getSentMail() carefully.

Make sure it retrieves enough information for commitment extraction:

- id
- subject
- body
- bodyPreview if available
- sentDateTime
- toRecipients
- ccRecipients
- webLink
- conversationId

Prefer text body content where possible.

Do not remove the existing working Graph functionality.

====================================================
DEBUGGING REQUIREMENT
====================================================

Add useful backend logging while fixing this.

I want logs such as:

COMMITMENT REQUEST
Sent emails: X
Inbox emails: X

AI commitment extraction started

AI commitments returned: X

Fallback commitments detected: X

FINAL COMMITMENT COUNT: X

FINAL SUMMARY: {...}

For every final commitment log:

COMMITMENT:
title
status
originalQuote
sourceSubject

Do not log sensitive full email bodies unnecessarily.

====================================================
IMPORTANT ARCHITECTURE REQUIREMENT
====================================================

Do NOT solve this by simply changing:

const list = d.commitments || [];

to some fake frontend array.

The backend must actually return structured commitments.

The frontend should consume:

d.commitments

normally.

The solution should therefore work for any future real Outlook emails, not only the two current test emails.

====================================================
EXPECTED RESULT WITH CURRENT TEST DATA
====================================================

After the fix, when I click:

"Refresh analysis"

the two obvious commitment emails should be detected:

1.
"I will send you to tomorrow"

2.
"I'll sent to you soon"

The conversational email:

"Hi Ghosal, Sneha, Badam khayegi ?"

should NOT be detected as a commitment.

"Important testing part 2"

should NOT automatically become a commitment.

Therefore the page should no longer show:

Active 0
Due today 0
Overdue 0
Completed 0
No deadline 0
No commitments in this view

Instead, it should display the detected real commitments in the existing cards/UI.

====================================================
DO NOT MODIFY UNRELATED FEATURES
====================================================

Do not break:

- Outlook inbox
- Calendar
- AI Copilot
- email drafting
- email sending
- calendar actions
- MSAL authentication
- existing navigation
- existing Commitment UI styling
- existing human approval workflow

Do not modify main.jsx.

Only make the minimum necessary changes to:

- main.py
- Commit.jsx
- graph.js

and only if required.

====================================================
FINAL RESPONSE
====================================================

After making the changes:

1. Clearly explain the root cause you found.
2. List exactly which files you changed.
3. Show the important changed sections.
4. Explain how the data now flows from Graph → backend → commitments → UI.
5. Tell me exactly how to restart the backend/frontend and test it.
6. Do not give me a completely new project.
7. Do not redesign Commit.jsx.

Before finishing, verify that the response from:

POST /api/commitments

contains:

{
  "commitments": [...]
}

with at least one valid commitment for the current test emails.
